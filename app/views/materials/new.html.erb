<% provide(:title, 'Novo Material') %>

<%= render "layouts/header.html.erb" %>
<%= stylesheet_link_tag 'estilo', media: 'all', 'data-turbolinks-track' => true %>

<div class="row" id="log">
  <div class="col-md-6 col-md-offset-3">
    <fieldset class="id_fieldset">
      <legend><h2> Cadastro </h2></legend>
      <%= form_for(@user) do |f| %>
          <%= render 'shared/error_messages' %>

          <%= f.text_field :descricao, class: 'form-control id_login', placeholder: 'Nome' %><br>
          <%= f.text_field :tipo, class: 'form-control id_login', placeholder: 'Tipo' %><br>
          <%= f.text_field :patrimonio, class: 'form-control id_login', placeholder: 'Patrimônio' %><br>

          <%= f.submit "Criar conta", class: "btn btn-primary botEnviar" %>
      <% end %>
    </fieldset>
  </div>
</div>

